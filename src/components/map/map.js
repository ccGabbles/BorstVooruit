import React from 'react';
import PropTypes from 'prop-types';
import './map.scss';

const map = () => (
  <div className="map">
    map Component
  </div>
);

map.propTypes = {};

map.defaultProps = {};

export default map;
